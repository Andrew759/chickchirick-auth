services:
  nginx-auth:
    build: ./build/automated/nginx
    ports:
      - '81:80'
    depends_on:
      - api-auth-automated
    networks:
      - backend-auth

  api-auth-automated:
    build:
      context: .
      dockerfile: ./build/automated/Dockerfile-cache
    # Переменные окружения для ускорения BuildKit
    environment:
      - DOCKER_BUILDKIT=1
    ports:
      - "8081:8080"
    depends_on:
      postgres-auth-automated:
        condition: service_healthy
    # volumes:
    #   - .:/app
    networks:
      - backend-auth

  postgres-auth-automated:
    image: postgres:17.2
    container_name: postgres-automated
    hostname: postgres-automated
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASS}
    ports:
      - ${DB_PORT}:${DB_INTERNAL_PORT}
    networks:
      - backend-auth
    volumes:
      - db_data_automated:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U pguser_test" ]
      interval: 5s
      timeout: 3s
      retries: 5

#  redis-automated:
#    image: redis:latest
#    container_name: redis-automated
#    hostname: redis-automated
#    environment:
#      REDIS_USER: redis
#      REDIS_USER_PASSWORD: redispass
#    ports:
#      - "6385:6379"
#    volumes:
#      - redis_data_automated:/data
#    networks:
#      - backend-auth

networks:
  backend-auth:

volumes:
  db_data_automated:
  redis_data_automated: